interface:
  display_name: "RE MCP Stack Restore"
  short_description: "Restore revula/ghidra/jadx/android-rev MCP servers after restart"
  default_prompt: "Use $rev-mcp-restore to bring back the revula, ghidra, jadx, and android-rev MCP servers after a studio restart."
policy:
  allow_implicit_invocation: true
